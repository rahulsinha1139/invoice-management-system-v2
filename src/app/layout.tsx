import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "UEC Invoice Command Center | Terminal Interface",
  description: "Futuristic terminal-style invoice management system for Unique Engineers Corporation - GST compliant invoice generation with real-time tracking",
  keywords: ["invoice", "GST", "terminal", "command center", "unique engineers corporation"],
  authors: [{ name: "Unique Engineers Corporation" }],
  viewport: "width=device-width, initial-scale=1",
  themeColor: "#0a0a0a",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" suppressHydrationWarning>
      <head>
        <link rel="preconnect" href="https://fonts.googleapis.com" />
        <link rel="preconnect" href="https://fonts.gstatic.com" crossOrigin="anonymous" />
        <link
          href="https://fonts.googleapis.com/css2?family=JetBrains+Mono:ital,wght@0,400;0,500;0,600;0,700;1,400&display=swap"
          rel="stylesheet"
        />
      </head>
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
        suppressHydrationWarning
      >
        <div id="terminal-cursor" className="fixed top-0 left-0 w-2 h-5 bg-green-400 opacity-75 pointer-events-none z-50 animate-pulse hidden" />
        {children}
      </body>
    </html>
  );
}
